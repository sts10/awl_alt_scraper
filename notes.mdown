Some random nokogiri code

```ruby
  # doc = Nokogiri::HTML(open(this_page))
  # doc.search("div.reverse-chron__post").each do |post|
  #   post_url = post.children[3].children[1].attributes["href"].value
  # end

#   doc.search("div.post__body div p:first img:first").each do |img_html|
#     post_images << PostImage.new(img_html)
#   end
```
